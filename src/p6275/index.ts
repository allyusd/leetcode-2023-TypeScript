export function exportWarp(nums1: number[], nums2: number[], k: number): number {
  return minOperations(nums1, nums2, k);
}

// 2023-01-21
// 6275. Minimum Operations to Make Array Equal II
function minOperations(nums1: number[], nums2: number[], k: number): number {
  return -1;
}
